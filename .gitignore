# --- Secrets ---
# Never commit real credentials. .env.example is the tracked template.
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*

# --- Node / Next.js ---
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
.eslintcache

# --- Test & load-test artifacts ---
playwright-report/
test-results/
blob-report/
k6-results/
*.k6-summary.json

# --- Local data ---
# Mock ultrasound frames, cine manifests, and reports generated by the seed script.
seed-data/generated/
*.db
*.sqlite3

# --- Agent tooling ---
# Vendored third-party agent skills. skills-lock.json is tracked so they can be
# restored with `npx skills add`, but the fetched content stays out of the tree.
.agents/
.claude/skills/

# --- Tooling & OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
.vercel
